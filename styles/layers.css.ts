import { globalLayer } from '@vanilla-extract/css';

export const components = globalLayer('components');
export const slices = globalLayer('slices');
export const utilities = globalLayer('utilities');
